default:
    extensions:
        Behat\Symfony2Extension:
            kernel:
                path: app/BehatKernel.php
                class: BehatKernel
        Behat\MinkExtension:
            base_url: %base_url_selenium%
            browser_name: Firefox
            show_cmd: firefox %s
            sessions:
                default:
                    selenium2:
                        browser: chrome
